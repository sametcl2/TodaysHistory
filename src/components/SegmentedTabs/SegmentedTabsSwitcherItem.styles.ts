import { createStyles } from 'theme'

export const useSegmentedTabsSwitcherItemStyles = createStyles(() => ({
  item: {
    overflow: 'hidden',
    marginRight: 10,
    padding: 8,
    borderRadius: 10
  }
}))
