import { createStyles } from 'theme'

export const useRemoveAllFavroitesButtonStyles = createStyles(() => ({
  container: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center'
  }
}))
